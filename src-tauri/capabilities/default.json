{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main Nexus Notes window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
